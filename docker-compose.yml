services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: marketing_request_app
    volumes:
      - .:/var/www/html
    depends_on:
      - db
    networks:
      - marketing-network

  nginx:
    image: nginx:stable-alpine
    ports:
      - "8084:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - marketing-network

  db:
    image: mysql:8.0
    ports:
      - "3310:3306"
    environment:
      MYSQL_DATABASE: marketing_request
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - marketing-network

networks:
  marketing-network:
    driver: bridge

volumes:
  db_data:
